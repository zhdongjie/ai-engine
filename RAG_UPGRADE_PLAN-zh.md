# RAG 升级计划

## 文档目的

本文档定义了当前 `ai-engine` RAG 堆栈的升级路径。

它旨在作为未来变更的唯一实现参考。

本计划特意使用英文编写，因为当前环境可能会将中文文本显示为乱码。

## 实现约束

所有基于本文档的未来代码变更都应遵循以下规则：

1. 尽可能使用英文编写注释、日志消息、Markdown 文档、提示描述和新标识符。

2. 保留现有项目风格和架构，而不是引入新的编码风格。

3. 逐步应用变更，每个阶段都应可独立审查和可逆。

4. 优先扩展当前的检索管道，而不是一次性完全替换。

5. 保留功能标志或配置开关，以免部署影响答案质量。

## 当前项目基线

当前项目已具备一个可用的第一阶段 RAG 流水线：

- `src/ai_engine/chains/nodes/router_node.py` 中的基于提示的意图路由

- `src/ai_engine/chains/nodes/rag_node.py` 中的向量检索

- `src/ai_engine/utils/retrieval_utils.py` 中的重排序

- `scripts/init_knowledge_db.py` 中的知识摄取

- `resource/prompts` 中的业务特定提示分离

- `resource/knowledge` 中的业务特定知识分离

当前优势：

- 清晰的 FastAPI + LangServe 服务架构

- 现有的 RAG 入口点是集中式的

- 现有的重排序阶段已存在

- 知识摄取由脚本驱动，可以进行扩展

当前局限性：

- 检索仍然主要采用单路径向量检索

-检索前的查询理解能力较弱

- 块上下文信息不足以处理复杂的规则型文档

- 检索质量控制有限

- 该流程尚未将词汇和语义召回相结合

## 技术评估

以下部分将根据当前项目状态评估您列出的各项技术。

### 保留为基线

#### 固定大小分块

状态：

- 已实现

保留理由：

- 稳定

- 成本低

- 良好的回退机制

不单独依赖它的原因：

- 可能对语义单元的分割效果不佳

- 可能丢失策略、教程和常见问题解答文档的章节级含义

决定：

- 保留为基线回退分块策略

### 优先升级方案

#### 查询转换

价值：

- 非常高

为何适合本项目：

- 许多用户查询具有对话性、不完整或描述不足

- 当前的路由层可以识别意图，但并未真正优化检索查询

推荐功能：

- 查询重写

- 历史记录感知查询补全

- 多查询扩展

- 针对复杂请求的可选子查询分解

决定：

- 第一阶段优先级

#### 融合检索

定义：

- 结合语义检索和关键词检索，然后将结果与 RRF 融合

价值：

- 非常高

为何适用于此项目：

- 您的知识库包含政策条款、费用、产品名称、API 名称和精确短语

- 仅使用向量检索在精确关键词匹配方面表现不佳

推荐的融合堆栈：

- 使用 PGVector 或 Chroma 进行语义检索

- 使用 BM25 或等效的词汇检索

- 互惠排名融合

- 融合后使用现有的重排序器

决策：

- 第一阶段优先级

#### 上下文块标题

价值：

- 高

为何适用于此项目：

- 许多文档本身就具有层级结构

- 包含标题路径时，块会更有用

推荐用法：

- 将文档标题、章节标题、子章节标题和业务类型添加到块文本或元数据中

决策：

- 第一阶段优先级

#### 上下文增强检索

价值：

- 高

适用理由：

- 当前文本块可能匹配良好，但缺少相邻的解释性文本

- 比完整检索父文档更容易实现

推荐用法：

- 检索匹配的文本块

- 在最终构建上下文时，扩展前后同级文本块

决策：

- 第一阶段或第二阶段优先级

#### 由小到大检索

价值：

- 高

适用理由：

- 非常适合教程和规则文档

- 提高检索的精确度和生成时的完整性

推荐用法：

- 匹配小文本块

- 在生成之前解析到父文本块、父章节或父文档窗口

决策：

- 第二阶段优先级

#### 上下文压缩

价值：

- 高

适用理由：

- 添加上下文扩展后，原始提示上下文会变得冗余

- 压缩有助于仅保留最相关的证据

推荐用法：

- 融合后压缩并重新排序

- 最终提示组装前压缩

决策：

- 第二阶段优先级

#### 轻量级 CRAG

定义：

- 生成答案前进行检索质量检查

价值：

- 中等到高

为何适用于本项目：

- 可以阻止低置信度的检索结果直接进入答案生成阶段

- 减少低置信度答案

推荐的轻量级策略：

- 检查召回率

- Check 重排序分数阈值

- 检查分数分布

- 如果检索效果不佳，则采用宽松策略重试

- 如果证据仍然不足，则拒绝检索或优雅地降低检索优先级

决策：

- 阶段 2 优先级

### 稍后有用，但并非立即优先

#### 语义分块

价值：

- 中等到高

为什么不优先：

- 比固定分块更难调整

- 最好先优化检索策略

决策：

- 阶段 3

#### 文档增强

定义：

- 为分块生成合成问答对或检索提示

价值：

- 中等到高

为什么不优先：

- 需要离线生成质量控制

- 增加数据摄取复杂度

决策：

- 阶段 3

#### 相关片段提取 (RSE)

价值：

- 中等到高

为什么不优先：

- 它实际上是一个高级检索后处理框架

- 更好融合和压缩完成后

决策：

- 第三阶段

### 不建议作为早期投资

#### 自归零 (Self-RAG)

不推荐的原因：

- 过多的控制权委托给模型

- 调试难度较大

- 延迟较高

- 实现复杂度较高

决策：

- 并非当务之急

#### 知识图谱

不推荐的原因：

- 当前的知识模式仍然主要以文档为中心

- 图的构建和维护成本较高

决策：

- 并非当务之急

#### 层级索引

不推荐的原因：

- 当语料库规模大幅增长时，层级索引更有用

- 当前的瓶颈在于检索逻辑的质量，而非索引规模

决策：

- 并非当务之急

#### HyDE

不推荐的原因：

- 检索前需要额外调用一次模型

- 更适用于开放领域的检索，而非结构化业务知识

决策：

- 并非当务之急

#### 反馈循环

为何现在不做：

- 需要完整的在线反馈和评估循环

- 待检索核心稳定后再做更好

决定：

- 长期项目

## 选定的升级策略

推荐的升级路径为：

### 第一阶段：提升召回率

范围：

- 查询转换

- 融合检索

- 上下文块头

- 上下文增强检索

目标：

- 更一致地检索到正确的证据

为何此阶段优先：

- 检索质量是当前的主要瓶颈

- 这些更改可以在不强制重写整个架构的情况下提升质量

### 第二阶段：提升上下文质量

范围：

- 从小到大检索

- 上下文压缩

- 轻量级 CRAG

目标：

- 为生成器提供更清晰、更完整的上下文

为何此阶段次之：

- 仅提升召回率会增加上下文的大小和噪声

- 此阶段将良好的召回率转化为良好的最终结果提示

### 第三阶段：提升数据摄取和后处理质量

范围：

- 语义分块

- 文档增强

- RSE（检索系统增强）

目标：

- 提升分块质量和离线检索资源

此阶段安排在后半段的原因：

- 实现和评估成本更高

- 最好在主要的在线检索循环稳定后再添加

## 详细步骤计划

以下步骤特意详细说明，因为后续的实施工作应遵循此顺序。

### 步骤 1：引入检索策略配置

操作步骤：

- 在设置或提示配置中添加检索策略开关

- 允许启用或禁用：

- 查询重写

- 词法检索

- 融合检索

- 上下文扩展

- 压缩检索

- 检索质量检查

原因：

- 升级应循序渐进

- 如果答案质量下降，我们需要安全的回滚机制

预期影响：

- 更安全的部署

- 通过功能隔离，更易于调试

可能涉及的文件：

- `src/ai_engine/core/settings.py`

- `resource/prompts/*.yaml`

- 可能涉及 `src/ai_engine/core/constants.py`

### 步骤 2：添加查询转换层

操作步骤：

- 创建一个可重用的检索前查询转换模块

- 支持：

- 简单重写

- 历史记录感知重写

- 可选的多查询扩展

原因：

- 用户输入通常未针对检索进行优化

- 优化查询通常比更改嵌入模型带来更大的质量提升

预期影响：

- 提高对不完整或对话式输入的召回率

- 更好地处理后续问题

可能涉及的文件：

- `src/ai_engine/chains/common/` 下的新模块

- `src/ai_engine/chains/nodes/rag_node.py`

- `resource/prompts/intent_router.yaml` 或新的检索提示文件

### 第三步：添加词汇检索

操作步骤：

- 基于知识库构建一个轻量级的、基于 BM25 的检索器

- 保留语义检索作为现有的主要检索器

- 对同一个转换后的查询执行两个检索器

原因：

- 对于您的业务领域，精确的术语至关重要

- 词汇检索弥补了嵌入搜索的不足性能欠佳

预期影响：

- 精确短语、关键词、产品名称和标识符查询的性能提升

可能的文件：

- 新的检索实用程序模块

- 如果词汇索引需要离线资源，则使用 `scripts/init_knowledge_db.py`

- `src/ai_engine/chains/nodes/rag_node.py`

### 第 4 步：使用 RRF 融合语义和词汇结果

操作步骤：

- 实现倒数排名融合针对语义和 BM25 结果集进行重排序

- 在融合之后应用重排序器，而不是之前

原因：

- 融合可以在不过度依赖单一检索方式的情况下提高召回率

- RRF 简单且稳健

预期影响：

- 更均衡地平衡精确匹配和语义匹配查询的检索质量

可能的文件：

- `src/ai_engine/utils/retrieval_utils.py`

- `src/ai_engine/chains/nodes/rag_node.py`

### 第 5 步：使用头部路径丰富块上下文

操作步骤：

- 将章节路径信息存储在块元数据中

- （可选）在索引之前，将简洁的头部路径添加到块文本的前面

原因：

- 许多块的含义取决于它们的来源

- 带有头部信息的块更容易检索，也更容易被模型使用

预期影响：

- 提高章节级召回率

- 更好地理解答案

可能文件：

- `scripts/init_knowledge_db.py`

- `scripts/processors/` 目录下的处理器模块

### 步骤 6：添加上下文增强检索

操作步骤：

- 检索数据块时，同时获取相邻数据块

- 保持窗口大小可配置

原因：

- 最佳匹配的数据块通常缺乏周围的解释

- 相邻数据块扩展简单有效

预期影响：

- 生成答案的完整性更高

可能包含的文件：

- `scripts/init_knowledge_db.py`（用于数据块排序元数据）

- `src/ai_engine/utils/retrieval_utils.py`

- `src/ai_engine/chains/nodes/rag_node.py`

### 步骤 7：添加由小到大的检索

操作步骤：

- 在数据摄取过程中跟踪父子关系

- 使用小数据块进行检索

- 使用父级数据块构建最终上下文或父窗口

原因：

- 小块数据可以提高召回率

- 父上下文可以提高答案的完整性

预期影响：

- 在长规则文档和学习材料上获得更佳结果

可能的文件：

- `scripts/init_knowledge_db.py`

- 模式或元数据约定

- `src/ai_engine/utils/retrieval_utils.py`

### 步骤 8：添加上下文压缩

操作步骤：

- 在最终生成之前压缩扩展的上下文

- 移除低价值跨度，同时保留证据

原因：

- 不压缩的上下文扩展会增加噪声

- 压缩使更大上下文的检索成为可能

预期影响：

- 更好的最终提示质量

- 更少的词元浪费

可能的文件：

- 新的压缩工具

- `src/ai_engine/chains/nodes/rag_node.py`

### 步骤 9：添加轻量级 CRAG 检查

操作步骤：

- 在最终生成之前添加检索质量评分答案生成

- 如果质量较低：

- 尝试使用宽松的检索策略重试

- 或使用低置信度回退策略生成答案

原因：

- 弱检索不应直接导致高置信度答案

预期影响：

- 更高的可信度

- 在证据不足时更好地拒绝答案

可能的文件：

- `src/ai_engine/utils/retrieval_utils.py`

- `src/ai_engine/chains/nodes/rag_node.py`

### 步骤 10：重新评估分块策略

操作步骤：

- 在线检索路径稳定后，评估语义分块

- 与现有的固定大小分块进行比较

原因：

- 分块质量固然重要，但检索控制更为重要

预期影响：

- 可能提高检索精度

- 更高的数据摄取复杂度

可能的文件：

- `scripts/init_knowledge_db.py`

- 处理器模块

## 推荐的文件级重构方向

本节将重构计划与当前代码库进行映射。

### 检索编排

主文件：

- `src/ai_engine/chains/nodes/rag_node.py`

升级后的预期功能：

- 查询转换

- 多检索器执行

- 融合

- 重排序

- 质量检查

- 上下文扩展

- 压缩

- 生成最终答案

建议：

- 保留此处的编排功能

- 将可重用的检索逻辑移至辅助模块

- 不要让此文件变成单体文件

### 检索工具

主文件：

- `src/ai_engine/utils/retrieval_utils.py`

升级后的预期功能：

- RRF 融合

- 分数归一化

- 重排序处理

- 检索质量检查

- 上下文组装辅助工具

建议：

- 首先扩展此模块

- 仅在模块过大时才进行拆分

### 数据导入管道

主文件：

- `scripts/init_knowledge_db.py`

升级后的预期职责：

- 数据块元数据增强

- 父子元数据

- 数据块顺序元数据

- 可选的头部注入

- 可选的合成检索资源

建议：

- 将数据摄取集中在此处

- 将文档特定的规则移至处理器层

### 处理器层

主目录：

- `scripts/processors/`

升级后的预期职责：

- 文档特定元​​数据提取

- 语言提取

- 章节路径提取

- 可选的 QA 增强

建议：

- 将业务特定的转换放在此处

## 实现设计原则

### 原则 1：不要一次性替换当前的 RAG 流程

原因：

- 您已经有一个可用的管道

- 完全替换会增加回归风险

方法：

- 添加功能标志

- 在现有重排序路径周围添加新的检索阶段

### 原则 2：保持检索的确定性可能

原因：

- 模型博士即使是控制循环也更难调试

方法：

- 在引入自反检索之前，使用明确的阈值和分阶段的回退机制

### 原则 3：保持摄取元数据丰富

原因：

- 更好的检索质量始于更好的离线数据结构

方法：

- 添加以下结构化元数据：

- 语言

- 业务类型

- 文档名称

- 标题路径

- 数据块位置

- 父块引用

### 原则 4：将检索与生成问题分离

原因：

- 检索问题应该可以在不更改生成提示的情况下进行诊断

方法：

- 保持检索工具的模块化

- 清晰地记录检索步骤

- 保持提示逻辑独立于检索评分逻辑

## 本项目建议的发布顺序

实施顺序应为：

1. 检索功能标志

2. 查询转换

3. 词法检索

4. RRF 融合

5. 头部信息增强数据块元数据

6. 上下文增强

7. 压缩

8. 轻量级 CRAG

9. 由小到大的检索

10. 高级数据摄取改进

此顺序的原因：

- 每一步都建立在前一步稳定的基础上

- 最大程度地减少了代码重写

- 提供了可衡量的质量检查点

## 下一个里程碑

此项目的最佳下一个里程碑是：

### 里程碑 A：检索质量升级

交付成果：

- 查询转换

- 通过 RRF 实现 Vector + BM25 融合

- 增强头部信息的块

- 增强上下文信息的检索

为何应优先考虑：

- 相对于复杂度而言，影响最大

- 直接解决当前检索的不足之处

- 与现有代码库完美兼容

## 超出首次升级周期范围

以下内容不应包含在首次升级周期中：

- 知识图谱集成

- 自 RAG 控制器循环

- 完整的反馈学习循环

- 基于 HyDE 的检索

- 大规模索引层级重构

原因：

- 这些功能复杂度较高，但对当前代码库而言短期收益较低

## 最终建议

对于本项目，最切实可行的升级方案是：

- 首先改进证据检索方式

- 然后改进证据组装方式

- 然后改进证据压缩和验证方式

- 只有在这些改进之后，才考虑更高级的自主检索行为

此顺序与当前代码库相匹配，风险可控，并且应该能够最快地带来显著的质量提升。